# Example configuration file
# Copy this to application.conf and configure your settings
# NEVER commit application.conf with real values!

ktor {
    deployment {
        port = 8080
        # Bind to all interfaces (use specific IP in production)
        host = "0.0.0.0"
    }
    application {
        modules = [ com.portal.server.ApplicationKt.module ]
    }
}

# For development/testing
# ws://localhost:8080/ws

# For production with domain:
# wss://your-domain.com/ws